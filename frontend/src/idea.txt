1. fix the username at the top of the chatroom , make it fix
2. fix the input area in the chatroom as fix, doesn't depend on how much the chat is long , it should be fix.
3. 












-----------------------backend------------
1. fetchChatIfMissing--> for implement this function I need the currentChatId and I will pass this as params on api call and get the chat and now I can store that ChatId as "selectedChat" and use it further.